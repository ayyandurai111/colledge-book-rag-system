# Required
NVIDIA_API_KEY=your_nvidia_api_key_here

# Optional — API key auth (leave empty to disable)
RAG_API_KEY=

# Optional — file upload limit in MB (default 100)
MAX_FILE_MB=100

# Optional
LOG_LEVEL=INFO
ENV=development
